# qa_AI_agent 学习代码



## 网络契约

### 基础概念

* **Python web**

  * 用 Python 语言来写网页服务器程序。平时我们用的网页是“前端”（给用户看的界面），而 **Python Web** 就是在幕后处理数据的“大脑”（服务器）

* **HTTP 协议**

  * 浏览器（前端）和服务器（后端）之间说话的“规范和语言”。就好比两个人打电话，必须约定好怎么开头、怎么说话、怎么挂断，HTTP 就是这个通话规则

* **POST（请求方式）**

  * HTTP 规定了很多种请求动作。**GET** 像是“去图书馆查资料”（只看数据不修改）；而 **POST** 像是“去登记填表”（要把你输入的新数据**提交、发送**给服务器存起来）。注册和登录必须用 POST

* **JSON（数据格式）**

  * 前后端之间传递数据的**标准包裹格式**。它本质上长得像一个字典，比如：`{"username": "Alice", "password": "123"}`。前后端都看得懂它，传数据时全靠它

* **状态码（Status Code）**

  * `200`：成功（一切正常）
  * `400`：客户端传的数据有错（比如格式不对）
  * `500`：服务器内部代码崩溃了

* **服务器（Server）**

  * 一台**24小时不关机、连着网的电脑**（或者云主机），它在幕后默默运行，专门负责接收别人的请求、处理数据

* **Flask**

  * 运行在服务器上的一套**Python 软件框架**。
  * 它就像服务器的“大堂经理”，专门负责接听前端发来的请求，指挥 Python 代码去查数据库或处理业务，然后再把结果回传。

* **React**：

  * 运行在你电脑浏览器里的**前端框架**。
  * 它专门负责画出好看的按钮、输入框（用户界面），也就是用户能直接看到和点到的“前台”

* **路由（Route）**

  * 路由就是“网址路径”**，或者说服务器里的**“交通路线导航”
  * 在浏览器里输入网址，或者前端发起请求时，总得指定一个具体的地址（比如 `http://localhost:5000/api/login`）。这个 `/api/login` 就是一个**路由**。它告诉服务器你想去哪个功能板块

* **路由规则（Routing Rules）**

  * 路由规则就是服务器定下的“分流规章制度”
  * 我们用 `@app.route('/api/login')` 这种语法来制定规则。它的意思就是给服务器立下规矩
    * 如果有人访问 `/api/login`，立刻把请求交个这个名叫 `login` 的 Python 函数处理
    * 如果有人访问 `/api/register`，就交给 `register` 函数去处理

* **监听（Listening）**

  * 监听就是服务器“竖起耳朵、24小时待命”的状态
  * 服务器程序（Flask）启动后，它不能自己干完就退出了。它会一直挂在电脑的某个端口（比如 `5000` 端口）上

  

### 交互运行逻辑

#### 前端打包 （React 侧）

* **收集数据**
  * 在输入框里面输入了账号和密码
  * React把这两个数据打包在JSON格式里面
* **写快递单（构建 HTTP 请求）**
  * **请求方式（Method）**：写上 **POST**
  * **目标地址（URL）**：写上服务器的收件地址
  * **内容类型（Headers）**：贴上标签，告诉服务器：“我里面装的是 JSON 数据”
* **发货（发送请求）**
  * React 调用 **Axios** 库，把这个包裹通过网线（HTTP 协议）射向 Flask 服务器



#### 服务器接收与处理（Flask 侧）

* **大堂经理签收（Flask 路由拦截）**
  * Flask 服务器一直守在 `5000` 端口。
  * 它看到一个来自前端的 POST 请求，地址是 `/api/register`
  * 立刻指派对应的 Python 函数来拆快递
* **拆包拿数据（解析 JSON）**
  * Python 代码运行 `request.get_json()`，把箱子打开，拿到了 `username` 和 `password` 的值
* **业务逻辑与漏洞（存数据库）**
  * Python 代码本该检查用户名是不是满 6 位（但项目要求这里**故意不写**这个校验）
  * Python 把 `tom` 和 `123` 塞进 SQLite 数据库的 `users` 表里存好



#### 服务器回信（Flask 侧）

* **打包回执（构建 Response）**
  * Python 决定结果是成功还是失败。
  * 它准备好一个回信箱（也是 JSON）：`{"message": "注册成功", "code": 200}`
  * 同时盖上一个 **HTTP 状态码**戳记：**`200`**（代表一切正常成功）
* **寄回前端（发送响应）**
  * Flask 把这个回信包裹通过 HTTP 原路送回给浏览器



#### 前端收信与展示（React 侧）

* **浏览器签收与拆信**：
  * React 收到服务器回传的包裹，打开一看，状态码是 `200`，信里写着“注册成功”
* **界面变化（渲染）**：
  * React 触发前端界面的更新，弹出一个绿色的提示框“注册成功！”，并自动帮你跳转到登录页面。



#### AI插件功能

* **喂给 AI 一份 PRD**：你把写好的产品需求文档（PRD）丢给 AI
* **AI 自动解析需求**：AI 读懂 PRD 里的规则（比如：用户名要 $\ge 6$ 位）
* **AI 自动生成用例**：AI 自动帮你写好测试用例（比如测试输入 `abc` 时，系统应该报错）
* **AI 自动执行与捉虫**：AI 去调你写的 Flask 接口，输入 `abc`。结果发现后端居然成功注册了（因为你故意埋了 Bug 没有做校验）
* **AI 自动提 Bug**：AI 发现“实际结果（注册成功）”和“PRD要求（$\ge 6$位报错）”对不上，于是**自动生成一张标准的 Bug 单**，推送出去





### 编写代码

1. **后端底座**

* 安装 **Flask**：让 Python 变成网页服务器的软件库。
* 安装 **Flask-Cors**：因为你的前端（React）和后端（Flask）以后可能是分开跑在不同端口上的，浏览器出于安全默认会拦截，这个工具用来**允许跨域访问**（打通前后端通信的绿色通道）
* 建一个用来存数据的文件数据库
* 把 Flask 启动起来，让它变成一个 24 小时开门营业的“快递站”
* **写好两个 HTTP 接口与处理函数（路由 + 业务代码）**
  * **注册接口（`POST /api/register`）**
    * **路由规则**：定好暗号。如果前端发来一个 `POST` 请求，地址是 `/api/register`，就指派“注册处理函数”来接客
    * **处理逻辑**：
      * **拆快递**：从前端发来的 JSON 里，把 `username` 和 `password` 拿出来。
      * **存入数据库**：把拿到的账号密码用 SQL 语句（`INSERT INTO`）塞进刚才建好的 SQLite 表里。
      * **回信**：打包一个成功的 JSON 回执（比如 `{"message": "注册成功", "code": 200}`）和状态码 `200`，原路送回给前端。
  * **登录接口（`POST /api/login`）**：
    * **路由规则**：地址定为 `/api/login`。







`create vite` 的意义

* `Vite` 是一个**前端项目的脚手架工具**（也可以理解为一个“项目生成器”）
  * **如果没有它**：你需要自己手动去新建文件夹、写 HTML、配 JavaScript、配打包工具、配浏览器实时刷新……光是把项目跑起来就要折腾半天
  * **有了它**：它会瞬间为你搭好一个**开箱即用、结构规范、性能极高**的现代化前端项目骨架，让你直接专注于写业务代码
* **Framework（选择前端框架）**
  * 是**写代码的工具集和架构**
  * 规定了你如何用“组件”去拼出一个网页，帮你高效管理页面的数据和交互
* **Variant（选择变体/语言）**
  * **写代码时用的文法规则**
* **Linter（代码检查工具）**



**frontend 顺序**

* **`utils/request.js`**（发请求的工具）
* **`api/auth.js`**（接口：登录/注册）—— 这一步才会用到返回的 token
* **`context/AuthContext.jsx`**（全局状态）—— **在这里写 localStorage**：登录成功 → 存 token；页面刷新 → 读 token 恢复登录态
* **页面**（Login / Register / Home）
* **交互**（点按钮 → 调用 AuthContext 里的方法）
* **调接口**（最终页面请求走）











